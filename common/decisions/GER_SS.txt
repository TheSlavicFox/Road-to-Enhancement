TFF_the_wehrmacht_purge = {

  TFF_akte_rommel = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 10

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.2
      }
		}
  }

  TFF_akte_von_manstein = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 40

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.3
      }
		}
  }

  TFF_akte_guderian = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 20

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.4
      }
		}
  }

  TFF_akte_von_bock = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 20

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.5
      }
		}
  }

  TFF_akte_von_weichs = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 20

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.6
      }
		}
  }

  TFF_akte_von_rundstedt = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 40

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.7
      }
		}
  }

  TFF_akte_kesselring = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 10

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.8
      }
		}
  }

  TFF_akte_raeder = {

		icon = GFX_decision_icon_default

		available = {
		}

    cost = 10

		fire_only_once = yes

		visible = {
		  always = yes
		}

    complete_effect = {
      country_event = {
        id = tff.9
      }
		}
  }

  TFF_the_wehrmacht_coup = {

		icon = GFX_decision_generic_army_support

		available = {
      check_variable = {
        var = ss_legitimacy
        value = 80
        compare = greater_than_or_equals
      }
		}

    activation = {
      has_country_flag = wehrmacht_purge_activated
    }

		fire_only_once = yes

    is_good = no
    days_mission_timeout = 90

		visible = {
		}

    timeout_effect = {
      hidden_effect = {
        set_country_flag = wehrmacht_purge_successful
        clr_country_flag = wehrmacht_purge_activated
      }
      add_ideas = aftermath_of_the_wehrmacht_purge
      start_civil_war = {
        ideology = neutrality
        size = 0.4
        keep_political_leader = yes
        keep_unit_leaders = { 35 }
      }
      remove_unit_leader = 11
      random_other_country = {
  			limit = {
  				original_tag = GER
  				has_government = neutrality
  			}
  			set_cosmetic_tag = GER_provisional_gvt
        create_country_leader = {
          name = "Maximilian von Weichs"
        	desc = "POLITICS_MAXIMILIAN_VON_WEICHS_DESC"
        	picture = "Portrait_Germany_Maximilian_von_Weichs.dds"
        	expire = "1965.1.1"
        	ideology = despotism
        	traits = {
        	}
        }
      }
      news_event = {
        id = wtt_news.1
      }
		}
    complete_effect = {
      country_event = {
          id = tff.10
      }
    }
  }
}
